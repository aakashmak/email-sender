name: Send Campaign Emails

on:
  schedule:
    # Run weekdays at 9:17 AM CST (15:17 UTC)
    # Using minute 17 to avoid high-load times at start of hour
    - cron: '17 15 * * 1-5'
    # Backup run 1 hour later — covers GitHub runner-allocation failures.
    # Send runner is idempotent (already-sent contacts skip), so safe to double-fire.
    - cron: '17 16 * * 1-5'

  # Allow manual triggers from GitHub Actions UI
  workflow_dispatch:

jobs:
  send-emails:
    runs-on: ubuntu-latest
    environment: email-sender  # Uses secrets from this GitHub environment

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install

      - name: Download resumes from Google Drive
        run: |
          mkdir -p temp
          curl -L "https://drive.google.com/uc?export=download&id=${{ secrets.RESUME_GDRIVE_DATA_SCIENTIST }}" -o temp/Resume_DataScientist.pdf
          curl -L "https://drive.google.com/uc?export=download&id=${{ secrets.RESUME_GDRIVE_DATA_ANALYST }}" -o temp/Resume_DataAnalyst.pdf
          curl -L "https://drive.google.com/uc?export=download&id=${{ secrets.RESUME_GDRIVE_BUSINESS_ANALYST }}" -o temp/Resume_BusinessAnalyst.pdf
          curl -L "https://drive.google.com/uc?export=download&id=${{ secrets.RESUME_GDRIVE_AI }}" -o temp/Resume_AI.pdf
          curl -L "https://drive.google.com/uc?export=download&id=${{ secrets.RESUME_GDRIVE_DATA_ENGINEER }}" -o temp/Resume_DataEngineer.pdf
          echo "Resumes downloaded: $(ls -la temp/)"

      - name: Send approved emails
        env:
          GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
          GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
          GMAIL_REFRESH_TOKEN: ${{ secrets.GMAIL_REFRESH_TOKEN }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          DAILY_LIMIT: ${{ secrets.DAILY_LIMIT }}
          SENDER_NAME: ${{ secrets.SENDER_NAME }}
          SENDER_LINKEDIN: ${{ secrets.SENDER_LINKEDIN }}
          TRACKING_PIXEL_BASE_URL: ${{ secrets.TRACKING_PIXEL_BASE_URL }}
          FOLLOW_UP_INTERVALS: '3,7,10'
          MAX_FOLLOW_UPS: '3'
        run: npm run send
